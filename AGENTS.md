# Agent instructions

Repo role: **TRANSITIONAL skeleton** — will project sema records to `.rs` + `Cargo.toml` + `flake.nix` for nix to consume. One-way emission: sema → Rust source.

Read [ARCHITECTURE.md](ARCHITECTURE.md) for the projection shape.

Workspace conventions live in [mentci/AGENTS.md](https://github.com/LiGoldragon/mentci/blob/main/AGENTS.md).

**Status: skeleton, no active development.** Body lands when criome's record supply is wide enough to project. Don't refactor or scaffold further until then.

The crate name `rsc` is provisional — the renaming-to-full-English bd issue is `mentci-next-dqp`.
