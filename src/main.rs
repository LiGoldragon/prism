//! rsc — projector from sema database records to Rust source.
//!
//! Walks an opus (a compilation unit in the database), emits a
//! directory of `.rs` files, and (optionally) invokes `cargo build`
//! in a temporary crate directory.

fn main() -> anyhow::Result<()> {
    Ok(())
}
