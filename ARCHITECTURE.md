# ARCHITECTURE — rsc

A future projector from records to Rust source. Walks records
in sema, emits a `.rs` tree plus the build glue, hands off to
lojix to compile.

Stub today. The project will be defined once the engine has
records to project.
